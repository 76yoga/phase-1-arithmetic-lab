
// Write your code
let num1 = 31;
let num2 = 2;

// Requirement 1: Create a variable called multiply set to an equation that will multiply the variables num1 and num2; the result will equal 62.

let multiply = num1 * num2;

// Requirement 2: Create a variable called random that will generate a random integer greater than 0.
let random = Math.floor(Math.random() * 10) + 1;

let num3 = 9;
let num4 = 5;
// Requirement 3: Create a variable called mod set to an equation tha
let mod = num3 % num4;

// Requirement 4: Create a variable called max that finds the highest number in a set; the value returned should be 20.
let max = Math.max(2, 5, 6, 10, 20);

console.log(multiply); // Output: 62
console.log(random); // Output: Random integer greater than 0
console.log(mod); // Output: 4
console.log(max); // Output: 20
